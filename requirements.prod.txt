# ScoutBase Africa — Production Dependencies
# CPU-only (GPU inference offloaded to Modal)

# Core ML / Vision
ultralytics>=8.0.0
supervision>=0.18.0
opencv-python>=4.8.0

# API Server
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Database (PostgreSQL via asyncpg)
asyncpg>=0.29.0
supabase>=2.0.0

# AI Report Generation
google-generativeai>=0.3.0
anthropic>=0.18.0

# Data
numpy>=1.24.0

# SAM3 (local CPU fallback + Modal offload)
transformers>=5.0.0
huggingface-hub>=0.20.0
safetensors>=0.4.0
Pillow>=9.0.0

# Modal (serverless GPU for SAM3)
modal>=0.67.0

# Observability
langfuse>=2.0.0

# Auth (Supabase JWT validation)
PyJWT>=2.8.0
cryptography>=42.0.0

# HTTP client (for Roboflow Inference Server)
httpx>=0.27.0
